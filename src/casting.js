"use strict";
var casting;
(function (casting) {
    let idade = 25;
    idade.toFixed(); // tratar essa variavel como (as) um numero (number)
    idade.length;
    idade.forEach(x => {
        console.log(x);
    });
    let nome = 35; // Conversão de atributos
})(casting || (casting = {}));
